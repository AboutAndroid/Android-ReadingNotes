<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:appcompat="http://schemas.android.com/apk/res-auto">

    <!--
    android:orderInCategory="80" menu_item 的排序，越大越靠下
    没有这个属性按照 xml 中书写的顺序排在加了这个属性的 item 上面
    -->

    <item
        android:id="@+id/home"
        android:icon="@drawable/ic_bottombar_home"
        android:orderInCategory="80"
        android:title="首页"
        android:visible="true"
        appcompat:showAsAction="never"/>

    <item
        android:id="@+id/financing"
        android:icon="@drawable/ic_bottombar_financing"
        android:title="理财"
        android:visible="true"
        appcompat:showAsAction="ifRoom"/>

    <item
        android:id="@+id/feedback"
        android:title="反馈意见"
        android:visible="true"
        appcompat:showAsAction="never"/>

    <item
        android:id="@+id/about"
        android:orderInCategory="90"
        android:title="关于"
        android:visible="true"
        appcompat:showAsAction="never"/>

</menu>